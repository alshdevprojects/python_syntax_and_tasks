# clear() - removes all elements from the dictionary
# copy() - returns a copy of the dictionary
# classmethod dict.fromkeys(seq[, value]) - creates a dictionary with the keys in seq and the value (default is None)
# get(key[, default]) - returns the value of the key or, if the key is not in the dictionary, the default value
# items() - Returns a sequence of tuples (key, value)
# keys() - returns the keys of the dictionary as a sequence
# pop(key[, default]) - removes a key and returns the value. If the key does not exist, returns default
#       (by default, throws an exception).
# popitem() - removes and returns the last (key, value) pair added to the dictionary.
#           If the dictionary is empty, throws a KeyError exception
# setdefault(key[, default]) - returns the value of the key, but if it does not exist,
#              does not throw an exception, but creates a key with the value default (default is None)
# update([other]) - updates the dictionary by adding (key, value) pairs from other
# values() - returns the dictionary values as a sequence

