# methods

# append(x) - adds an element to the end of the list
# clear() - removes all values from the list
# copy() - returns a copy of the list
# count(x) - returns the number of occurrences of a value in the list
# extend(iterable) - appends elements from the specified iterable to the list
# index(x[, start[, end]]) - returns the index of the first element with value x
# insert(i, x) - inserts the specified element before the specified index in the list
# pop([i]) - returns the element at the specified position, removing it from the list
# remove(x) - removes the specified element from the list
# reverse() - reverses the order of the elements in the list
# sort(key=None, reverse=False) - sorts the elements of the list
